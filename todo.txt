
For 0.0.4
=========

Replace null value strategy with default value strategy.

Fantom value types

Int -> 0
Bool -> false 
Map
List
Str -> defVal (!)

If type is null, use null.

If not null, use defVal.


For 1.0.0
=========

use afBeanUtils::BeanFactory to create embedded entity objs with const fields

_version -> auto optimistic locking

PrePersist & PostPersist, PreLoad & PostLoad (better names?), configure via a Facet Interceptor -> tie in with Entity mixin?



For Future
==========

